
<div class="modal" id="customer-form">
  <div class="modal-dialog modal-md">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
      </div>
      <div class="modal-body">
        <%= form_tag customers_path, method: :post, class: 'form-horizontal' do %>
          <div class="form-group">
            <div class="col-sm-12">
              <%= label_tag :name, 'Nome', class: 'col-xs-6' %>
              <%= text_field_tag :name, '', class: 'form-control', placeholder: 'Digite o nome do cleinte', required: true %>
            </div>
            <div class="col-sm-12">
              <%= label_tag :document, 'CPF', class: 'col-xs-6' %>
              <%= text_field_tag :document, '', class: 'form-control', placeholder: 'Digite o CPF do cliente', required: true %>
            </div>
            <div class="col-sm-12">
            <%= label_tag :email, 'Email', class: 'col-xs-6' %>
              <%= text_field_tag :email, '', class: 'form-control', placeholder: 'Digite o email do cliente', required: true %>
            </div>
          </div>
          <%= submit_tag 'Salvar', class: 'btn btn-sm btn-primary pull-right' %>
        <% end %>
        <div class="clearfix"></div>
      </div>
    </div>
  </div>
</div>